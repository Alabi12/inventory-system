<p style="color: green"><%= notice %></p>

<h1>Purchase orders</h1>

<div id="purchase_orders">
  <% @purchase_orders.each do |purchase_order| %>
    <% @purchase_orders.each do |purchase_order| %>
  <p>
    <strong>Customer:</strong> <%= purchase_order.customer.name %>
    <strong>Supplier:</strong> <%= purchase_order.supplier.name %>
    <strong>Status:</strong> <%= purchase_order.status %>
  </p>
<% end %>
    <p>
      <%= link_to "Show this purchase order", purchase_order %>
    </p>
  <% end %>
</div>

<%= link_to "New purchase order", new_purchase_order_path %>
