<%= form_with(model: purchase_order) do |form| %>
  <% if purchase_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(purchase_order.errors.count, "error") %> prohibited this purchase_order from being saved:</h2>

      <ul>
        <% purchase_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :supplier_id, style: "display: block" %>
    <%= form.text_field :supplier_id %>
  </div>

<div class="field">
  <%= form.label :status %>
  <%= form.select :status, PurchaseOrder.statuses.keys.map { |status| [status.humanize, status] }, prompt: "Select Status" %>
</div>

  <div>
    <%= form.label :order_date, style: "display: block" %>
    <%= form.date_field :order_date %>
  </div>

  <div>
    <%= form.label :received_date, style: "display: block" %>
    <%= form.date_field :received_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
